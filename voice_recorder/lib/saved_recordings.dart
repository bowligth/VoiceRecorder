import 'package:flutter/material.dart';

class SavedRecordingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Saved Recordings'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Add widgets to list and play saved recordings here
          ],
        ),
      ),
    );
  }
}
